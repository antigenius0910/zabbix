# Images separated by architecture
images:
  # Contains list of images built for all architectures
  multi_arch: 
  # Contains list of images built only for the amd64 architecture
  amd64: genctl/zabbix
