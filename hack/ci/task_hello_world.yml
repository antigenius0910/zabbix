platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.14.15-alpine3.13

run:
  path: /bin/bash
  args:
  - -euc
  - |
    build_root="${PWD}"

    cd zabbix
    date
    uname -a 
    ls -al
